<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accueil</title>

    <style>
      body {
        /* background-color: #bd915a; */
        background-image: url("/assets/anneaux.jpg");
        background-size: cover; /* Redimensionner l'image pour couvrir tout le contenu */
        background-position: center; /* Centrer l'image horizontalement et verticalement */
      }

      main {
        margin: 5rem;
      }

      .card-columns {
        max-width: 800px;
        margin: 0 auto;
      }

      .card:hover {
        transform: translateY(-0.1rem);
      }
      .create { margin :0.8rem
      }

      .jumbotron{
      font-weight: 800;
      }


    </style>
  </head>

  <body>
    <!-- NAV -->
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Gossip Project</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <a class="nav-link" href="http://localhost:3000/">Accueil<span class="sr-only"></span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/team">Notre équipe</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/contact">Nous contacter</a>
            </li>
          </ul>
        </div>
      </nav>
    </header>

    <!-- MAIN -->

    <main>
      <div class="jumbotron">
        <h1 class="display-4">Bienvenue à toi fils du Rohan !</h1>
        <p class="lead">Voici les comtes de la Terre du Milieu :</p>
        <hr class="my-4">
        <p>“Un soleil rouge se lève. Beaucoup de sang a du couler cette nuit... ”</p>
      </div>

      <a class="btn btn-light btn-lg create" href="/form"> Créer un potin </a>
    

      <!-- Potins pré-enregistrés en BDD -->

      <div class="row">
        <% @gossips.each_with_index do |gossip| %>
          <div class="col-md-4 mb-4">
            <div class="card text-white bg-dark">
              <div class="card-header"><%= gossip.title %></div>
              <div class="card-body">
                <p class="card-text"><%= truncate(gossip.content, length:20, separator: ' ') %></p>
                <a class= "btn btn-light" href="<%=user_path(gossip.user.id)%>">Auteur</a>
                <%= link_to "Voir le potin", "/show_new_gossip/#{gossip.id}", class: "btn btn-light" %>
                <div class="card-footer card-text">
            Posté le <%= gossip.created_at.strftime("%d/%m/%Y") %>
          </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>

    </main>

  </body>
</html>